name: Release iOS

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-ios:
    name: Build & Release iOS
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        working-directory: .
        run: flutter pub get

      - name: Bump build number
        working-directory: .
        run: |
          # Version comes from the release tag (v2.2.3 → 2.2.3); pubspec is only a fallback.
          BASE="${GITHUB_REF_NAME#v}"
          echo "$BASE" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+$' || BASE=$(grep '^version:' pubspec.yaml | sed -E 's/^version: ([0-9.]+).*/\1/')
          NEW="${BASE}+${{ github.run_number }}"
          sed -i.bak -E "s/^version: .*/version: ${NEW}/" pubspec.yaml
          rm -f pubspec.yaml.bak
          echo "Build version: ${NEW}"

      - name: Install CocoaPods dependencies
        working-directory: ./ios
        run: pod install

      - name: Import signing certificate
        env:
          CERTIFICATE_P12_BASE64: ${{ secrets.IOS_P12_BASE64 }}
          CERTIFICATE_PASSWORD: ${{ secrets.IOS_P12_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.IOS_KEYCHAIN_PASSWORD }}
        run: |
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          echo -n "$CERTIFICATE_P12_BASE64" | base64 --decode > $RUNNER_TEMP/certificate.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security import $RUNNER_TEMP/certificate.p12 -P "$CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH
          echo "=== Imported certificates ==="
          security find-identity -v -p codesigning $KEYCHAIN_PATH

      - name: Install provisioning profile
        env:
          PROVISIONING_PROFILE_BASE64: ${{ secrets.IOS_PROVISION_PROFILE_BASE64 }}
        run: |
          PP_PATH=$RUNNER_TEMP/profile.mobileprovision
          echo -n "$PROVISIONING_PROFILE_BASE64" | base64 --decode > $PP_PATH
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          DECODED=$(security cms -D -i "$PP_PATH")
          UUID=$(echo "$DECODED" | plutil -extract UUID raw -)
          PROFILE_NAME=$(echo "$DECODED" | plutil -extract Name raw -)
          BUNDLE_ID=$(echo "$DECODED" | plutil -extract Entitlements.application-identifier raw - | sed 's/^[^.]*\.//')
          EXPIRY=$(echo "$DECODED" | plutil -extract ExpirationDate raw -)
          PROFILE_TYPE=$(echo "$DECODED" | plutil -extract ProvisionsAllDevices raw - 2>/dev/null && echo "Enterprise" || (echo "$DECODED" | plutil -extract Entitlements.get-task-allow raw - 2>/dev/null | grep -q true && echo "Development" || echo "Distribution/AppStore"))
          echo "=== Provisioning profile info ==="
          echo "Profile name:      $PROFILE_NAME"
          echo "Profile UUID:      $UUID"
          echo "Profile bundle ID: $BUNDLE_ID"
          echo "Profile expires:   $EXPIRY"
          echo "Profile type:      $PROFILE_TYPE"
          cp "$PP_PATH" ~/Library/MobileDevice/Provisioning\ Profiles/"$UUID".mobileprovision
          # The team the profile was actually issued to. IOS_TEAM_ID has been
          # empty or stale often enough that the profile is the better source.
          PROFILE_TEAM_ID=$(echo "$DECODED" | plutil -extract TeamIdentifier.0 raw -)
          echo "Profile team:      $PROFILE_TEAM_ID"
          echo "PROFILE_NAME=$PROFILE_NAME" >> $GITHUB_ENV
          echo "PROFILE_UUID=$UUID" >> $GITHUB_ENV
          echo "PROFILE_TEAM_ID=$PROFILE_TEAM_ID" >> $GITHUB_ENV

      - name: Inject profile UUID into project
        run: |
          # UUID rather than name: Xcode 26 on macos-26 does not find an
          # installed profile by name. Two expressions because the placeholder
          # may or may not still carry its quotes — `flutter build` normalises
          # the pbxproj — and quoting an already-quoted value corrupts it
          # ("project 'Runner' is damaged").
          sed -i '' \
            -e "s/\"CI_PROFILE_NAME\"/\"$PROFILE_UUID\"/g" \
            -e "s/CI_PROFILE_NAME/\"$PROFILE_UUID\"/g" \
            ./ios/Runner.xcodeproj/project.pbxproj
          grep PROVISIONING_PROFILE_SPECIFIER ./ios/Runner.xcodeproj/project.pbxproj

      - name: Build Flutter iOS (no codesign)
        working-directory: .
        run: flutter build ios --release --no-codesign

      - name: Archive with manual signing
        env:
          TEAM_ID: ${{ secrets.IOS_TEAM_ID }}
        working-directory: .
        run: |
          xcodebuild archive \
            -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -archivePath $RUNNER_TEMP/Runner.xcarchive \
            DEVELOPMENT_TEAM=${PROFILE_TEAM_ID:-$TEAM_ID}

      - name: Export IPA
        env:
          TEAM_ID: ${{ secrets.IOS_TEAM_ID }}
        run: |
          /usr/libexec/PlistBuddy -c "Set :teamID ${PROFILE_TEAM_ID:-$TEAM_ID}" ./ios/ExportOptions.plist
          /usr/libexec/PlistBuddy -c "Set :provisioningProfiles:com.lioilsources.beenhere $PROFILE_NAME" ./ios/ExportOptions.plist
          xcodebuild -exportArchive \
            -archivePath $RUNNER_TEMP/Runner.xcarchive \
            -exportOptionsPlist ./ios/ExportOptions.plist \
            -exportPath $RUNNER_TEMP/ipa

      # A crash report from a release build is unreadable without the debug
      # symbols of that exact binary, and the archive lives in RUNNER_TEMP for
      # the length of the job only — so every app frame in every TestFlight
      # crash so far symbolicated to a bare address. Ship the dSYMs with the
      # build they belong to; Xcode's Organizer and `atos` both take them.
      #
      # A missing dSYM warns instead of failing: the IPA is what the release is
      # for, and the same reasoning as the ordering below applies — nothing
      # secondary may cost us the artifact.
      - name: Package dSYMs
        run: |
          if [ -z "$(ls -A $RUNNER_TEMP/Runner.xcarchive/dSYMs 2>/dev/null)" ]; then
            echo "::warning::archive holds no dSYMs — check DEBUG_INFORMATION_FORMAT for Release"
            exit 0
          fi
          mkdir -p $RUNNER_TEMP/dsyms
          ditto -c -k --sequesterRsrc --keepParent \
            $RUNNER_TEMP/Runner.xcarchive/dSYMs \
            $RUNNER_TEMP/dsyms/BeenHere-${{ github.ref_name }}-iOS-dSYMs.zip

      # Publish the artifact before handing it to Apple. The upload below talks
      # to a service that fails on its own schedule — v2.3.3 died on an altool
      # "Defaults.properties couldn't be opened" after a perfectly good build,
      # and because it ran first, the release shipped without an IPA at all.
      - name: Upload IPA to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ runner.temp }}/ipa/*.ipa
            ${{ runner.temp }}/dsyms/*.zip
          name: BeenHere ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') }}

      # Non-fatal on purpose: a TestFlight outage is still reported on the run,
      # but it must not invalidate a release whose IPA is already published.
      - name: Upload to TestFlight
        continue-on-error: true
        env:
          API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          API_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
          API_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}
        run: |
          mkdir -p ~/.appstoreconnect/private_keys
          echo -n "$API_KEY_BASE64" | base64 --decode > ~/.appstoreconnect/private_keys/AuthKey_${API_KEY_ID}.p8
          xcrun altool --upload-app \
            --type ios \
            --file "$(ls $RUNNER_TEMP/ipa/*.ipa)" \
            --apiKey "$API_KEY_ID" \
            --apiIssuer "$API_ISSUER_ID"

      - name: Clean up keychain
        if: always()
        run: |
          security delete-keychain $RUNNER_TEMP/app-signing.keychain-db 2>/dev/null || true
          rm -f ~/.appstoreconnect/private_keys/AuthKey_*.p8
